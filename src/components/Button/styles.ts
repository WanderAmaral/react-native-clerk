import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    justifyContent: "center",
    flexDirection: "row",
    alignItems: "center",
    gap: 10,
    borderWidth: 2,
    padding: 10,
    borderRadius: 10,
    marginTop: 10,
    width: '100%'
  },
  icon: { fontSize: 20 },
  text: { fontSize: 18 },
});
